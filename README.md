## Quick start

```json
  "scripts": {
    "start": "node ./bin/www",
    "dev": "nodemon ./bin/www"
  },
```

Start with command: `npm run dev` or `npm start` without auto reloading when something has changed.

